//
// whoisthat
// =========
//
// A library for a WHOIS client
//
'use strict';

function createClient (opts) {
  var Server = require('./lib/client.js');
}

module.exports.createClient = createClient;
